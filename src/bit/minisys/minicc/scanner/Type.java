package bit.minisys.minicc.scanner;
/*** for more readable and extendable thought, here choose enum, 
 * if want a better performance should use static constant instead
***/
public enum Type {

	IDENTIFIER, KEYWORD, OPERATOR, CONSTANT, SEPARATOR, WHITE_SPACE, END

}
